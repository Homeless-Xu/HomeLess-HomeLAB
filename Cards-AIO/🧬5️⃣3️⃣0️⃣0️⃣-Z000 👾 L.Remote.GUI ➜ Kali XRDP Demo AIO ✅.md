```
🔶 xrdp desc 

win   use rdp 
linux use vnc

xrdp. allow rdp tool to remote linux. 



🔶 xrdp config 

sudo apt install xrdp -y
sudo systemctl enable xrdp
sudo systemctl start xrdp


🔶 user Desc
one user can not login use cli(ssh)/gui(rdp) at same time.
so one use for ssh, another one for rdp.
no need create new user.
enable root for rdp. (just need add password to root)


