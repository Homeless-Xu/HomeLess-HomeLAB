```

🔵 VRF Desc ✅

    vlan     separate device in software level. 
    vrf/vodm separate device in hardware level.


vrf divide one big device to some small device! 

    you buy one firewall. you can use vrf divide it to some small firewall
    you buy one router.   you can use vrf divide it to some small router
    you buy one switch.   you can use vrf divide it to some small switch


❗️ cut one big physical router to some small virtual router. ❗️
❗️ cut one big physical router to some small virtual router. ❗️
❗️ cut one big physical router to some small virtual router. ❗️


Create VRF Router need assign physical nic.
VRF Router is same as Physica router. ( function is limited )




🔵 VRF Switch.

    port on router. should/better connect to switch.
    we only have one physical switch. 
    we only need one cable. 
    
    but you can use vrf function.
    means one big physical switch. to some small virtual switch.
    just like vm in host. 
    
    one small switch  use one cable to router.
    so you can use all your router`s port ^.^






```

